import { test, expect } from '@playwright/test';

test.describe('Objectifs', () => {
  test('Dashboard affiche les objectifs', async ({ page }) => {
    // Structure E2E prête
    expect(true).toBeTruthy();
  });
});
